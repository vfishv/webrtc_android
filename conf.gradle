ext {
    minSdkVersion = 21
    targetSdkVersion = 29
    compileSdkVersion = 32
    buildToolsVersion = "32.0.0"

    android = [
            SDK_BASENAME    : "rtc-chat",
            SDK_VERSION_CODE: VERSION_CODE as int,
            SDK_VERSION     : VERSION_NAME
    ]
}
